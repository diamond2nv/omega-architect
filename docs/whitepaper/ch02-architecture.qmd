---
chapter-id: ch02
chapter-version: 0.1.0
category: architecture
audience: [系统工程师]
keywords: [Inner Loop, Cadence, Gate, Feedback, Convergence Tracker, MCP]
git-commit: GIT_COMMIT_PLACEHOLDER
git-date: GIT_DATE_PLACEHOLDER
last-reviewed: GIT_DATE_PLACEHOLDER
reviewer: Ω-Architect Team
prerequisites: [Python asyncio, LLM tool_calls 概念]
depends-on: [ch01]
---

# 系统架构

## 整体架构图

```
User Query
    │
    ▼
┌───────────────────────────────────────────────┐
│              Inner Loop                        │
│                                                 │
│  ┌──────────┐    ┌──────────┐    ┌───────────┐ │
│  │ Cadence  │───▶│   Gate   │───▶│ Feedback  │ │
│  │ (retry)  │    │ (compile)│    │ (error→fix)││
│  └──────────┘    └──────────┘    └───────────┘ │
│       │               │               │        │
│       ▼               ▼               ▼        │
│  ┌──────────┐    ┌──────────┐    ┌───────────┐ │
│  │ DeepSeek │    │ Compile  │    │ MCP Tools │ │
│  │ API +    │    │ Gate     │    │ (leansea..│ │
│  │ tool_call│    │ (local)  │    │ loogle..) │ │
│  └──────────┘    └──────────┘    └───────────┘ │
│                                                 │
│  Converged? → return proof                      │
│  Stuck? → return with diagnosis                 │
└─────────────────────┬─────────────────────────┘
                      │
                      ▼
              ┌──────────────┐
              │ Dialogue     │
              │ Cache (JSONL)│
              └──────────────┘
```

## 模块分解

| 模块 | 文件 | 职责 |
|:-----|:-----|:-----|
| Inner Loop | `omega/loop/inner.py` | 主循环：搜索→代码→编译→反馈 |
| DeepSeek Client | `omega/loop/deepseek_client.py` | API 封装 + tool_calls + thinking |
| Compile Gate | `omega/loop/compile_gate.py` | 本地 `lean --stdin` + SHA256缓存 |
| Error Classifier | `omega/loop/errors.py` | 13 类错误分类 + 死循环检测 |
| MCP Client | `omega/loop/mcp_client.py` | lean-lsp-mcp 持久连接 |
| Dialogue Cache | `omega/loop/dialogue_cache.py` | 成功对话 JSONL 缓存 |
| Budget Tracker | `omega/resource/budget.py` | Token/时间/费用预算 |
| Convergence Tracker | `omega/resource/tracker.py` | Epoch级收敛/发散检测 |
